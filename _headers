/* Cloudflare Pages Recommendations:
  1. Content-Security-Policy: Recommended for security
  2. X-Content-Type-Options: nosniff
  3. X-Frame-Options: DENY
  4. Referrer-Policy: strict-origin-when-cross-origin
*/

/* Security Headers */
/* This file will be picked up by Cloudflare Pages to set HTTP headers */

/* Global Headers */
/* Set long-term caching for static assets */
/favicon.ico
  Cache-Control: public, max-age=604800, immutable
/logo.jpeg
  Cache-Control: public, max-age=604800, immutable
/style.css
  Cache-Control: public, max-age=3600
/script.js
  Cache-Control: public, max-age=3600

/* Security headers for all paths */
/* We'll apply these via the Cloudflare Dashboard or this file if supported for all assets */
/* Note: Cloudflare Pages uses this specific format for the _headers file */

/* Apply to all pages */
/* Since it's a simple site, we can be strict */
/* Note: lucide icons and google fonts are used, so we must allow those domains */

/* Root and all sub-pages */
/* Cloudflare Pages _headers syntax */
/* https://developers.cloudflare.com/pages/configuration/headers/ */

/* Basic Security Suite */
/* We'll use a standard set that works well for production */

/* Catch-all for all files */
/* Note: _headers applies to the actual paths matches */

/* We'll stick to a simple one that covers the main requirements */

/* Apply to all content */
/* We use wildcards */
/* Cloudflare headers are simple: path then indented headers */
/* We'll use the most common ones */

/* Rule for all pages */
/* Note: Cloudflare doesn't support wildcards in headers for ALL subfolders in the middle easily but the following usually works: */

/* For testing purposes and best practice: */
/index.html
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: no-referrer-when-downgrade
  Content-Security-Policy: default-src 'self'; script-src 'self' https://unpkg.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; img-src 'self' https://images.pexels.com data:;
